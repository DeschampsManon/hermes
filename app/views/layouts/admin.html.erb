<!DOCTYPE html>
<html>
    <head>
        <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,700" rel="stylesheet">
        <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
        <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
        <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
        <%= render 'layouts/meta', layout: 'admin', title: "Hermès | #{controller_name.titleize}" %>
    </head>
    <body data-locale='<%= I18n.locale %>' class="<%= "#{controller_name}-#{action_name}" %>">
        <div id="flash-alerts-container"><%= flash_messages_helper %></div>
        <%= render 'layouts/admin/main_header' %>
        <%= render 'layouts/admin/page_header' %>
        <div class="page">
            <%= yield %>
        </div>
    </body>
</html>
