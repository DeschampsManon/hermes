<!DOCTYPE html>
<html>
    <head>
        <%= render 'layouts/meta', layout: 'devise', title: "Hermès | #{resource_name.capitalize}" %>
    </head>
    <body>
        <section>
            <div class="container">
                <%= yield %>
            </div>
        </section>
    </body>
</html>
