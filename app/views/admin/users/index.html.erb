<div class="container">
    <table class="table-primary datatable display responsive no-wrap">
        <%= link_to t('new_user'), new_admin_user_path, class: 'btn btn-blue btn-new' %>
        <thead>
            <th class="no-sorting" width="160"><%= t 'username' %></th>
            <th><%= t 'name' %></th>
            <th><%= t 'email' %></th>
            <th width="70"><%= t 'roles' %></th>
            <th class="no-sorting" width="60"><%= t 'actions' %></th>
        </thead>
        <tbody>
            <% @users.each do |user| %>
                <tr>
                    <td>
                        <div class="table-thumbnail">
                            <%= image_tag user.avatar(:thumb), onerror:"this.onerror=null;this.src='#{image_url('no-avatar.png')}';" %>
                            <span><%= user.username %></span>
                        </div>
                    </td>
                    <td>
                        <%= user.first_name %>
                        <%= user.last_name %>
                    </td>
                    <td>
                        <%= link_to user.email, "mailto: #{user.email}" %>
                    </td>
                    <td>
                        <% user.roles.each do |role| %>
                            <%= role.name.capitalize() %>
                        <% end %>
                    </td>
                    <td>
                        <%= link_to material_icon.delete, user, class: 'btn btn-darkpurple', method: :delete, data: {confirm: t('confirm_destroy', title: user.username, controller_name: t('user'))} if current_user != user %>
                        <%= link_to material_icon.mode_edit, edit_admin_user_path(user), :class => 'btn btn-blue' %>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>